[build]
  base = "client"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# SPA routing - must come AFTER the API redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
